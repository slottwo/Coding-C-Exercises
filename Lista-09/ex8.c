#include <locale.h>
#include <stdio.h>

/*
Na função principal (após ter criado as 3 cidades), exiba as opções:
[1] - Listarcidades,
[2] - Alterar temperatura de cidade,
[0] - Sair.
Se o usuário digitar 1, exiba a lista de cidades para ele (com as informações de cada uma).
Após isso, exiba novamente a mensagem inicial com as 3 opções. Caso ele digite 2 , pergunte para 
ele o código da cidade e a quantidade de graus em celsius que ele deseja alterar. 
Atualize a temperatura da cidade (usando as funções de buscar cidade e alterar temperatura) e, após
isso, mostre novamente as opções iniciais para o usuário. Caso o usuário digite 0, finalize o programa.
*/

int main()
{
    setlocale(LC_ALL, "Portuguese");
    
    /* Code */
    
    return 0;
}
